/**
 * Flip EMPTY_STATE to true to make every repository return blank results,
 * so all screens render their designed empty states.
 */
export const EMPTY_STATE: boolean = false;

/** Simulated network latency (ms) for the mock repositories. */
export const MOCK_LATENCY_MS = 220;
